import { SearchStaffDesignationPipe } from './search-staff-designation.pipe';

describe('SearchStaffDesignationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStaffDesignationPipe();
    expect(pipe).toBeTruthy();
  });
});
