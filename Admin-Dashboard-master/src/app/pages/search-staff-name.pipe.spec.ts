import { SearchStaffNamePipe } from './search-staff-name.pipe';

describe('SearchStaffNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStaffNamePipe();
    expect(pipe).toBeTruthy();
  });
});
